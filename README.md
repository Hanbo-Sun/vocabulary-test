# vocabulary-test

Pure frontend vocabulary size tester.

Open `index.html` in a browser to run the test. Data lives in `data/en.json` so you can add future language packs without changing app logic.
